node_modules/
.env
.env.local
data/*.json
!data/.gitkeep
.DS_Store
*.log

# Generated by scripts/build-static.mjs - a fresh, deployable copy is
# rebuilt for every CI run and every deploy (see .github/workflows/*.yml).
dist/

# Throwaway verification artifacts written by build/QA scripts and by the
# headless-Chrome harnesses. Never part of a release.
scratch-*
**/_scratch_*
.scratch-*
.theme-verify-scratch/
